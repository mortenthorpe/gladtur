{% extends "GladturTagBundle::layout.html.twig" %}
{% block content %}
<h1>Opret sted</h1>

<form action="{{ path('location_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Opret nyt sted</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('location') }}">
            Til oversigt over steder
        </a>
    </li>
</ul>
{% endblock %}